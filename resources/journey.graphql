enum Episode {
  NEWHOPE
  EMPIRE
  JEDI
}

type Character {
  id: ID!
  name: String!
  episodes: [Episode!]!
}

type Query {
  character(id: ID!) : Character
  characters(episode: Episode!) : [Character!]!
}

schema {
  query: Query
}
